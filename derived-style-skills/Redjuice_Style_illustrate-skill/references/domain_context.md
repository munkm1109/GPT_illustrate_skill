# Domain Context

Summary: This derivative skill preserves `illustrate-skill` and applies a reference-derived cyber-gothic style pack extracted from the current `Reference-Redjuice` folder.

Summary: The current analysis basis is the `Reference-Redjuice` folder state on **2026-04-24**, which contains **29 JPG files**. This supersedes older local notes that mentioned 19 JPGs.

## User context

- User language in conversation: Korean
- Desired outcome: reusable reference-derived illustration planning skill based on this local pack
- Desired method: reference-based style learning encoded as reusable drawing rules, not model-weight training
- Base process to preserve: `illustrate-skill`

## Execution stance

- Use the base process skeleton from `illustrate-skill`.
- Use the local reference pack only to bias decisions inside each stage.
- Treat reference-derived fidelity as the primary style lock; secondary style requests are subordinate overlays unless the user explicitly replaces the wrapper.
- Keep evidence separate from procedure:
  - procedure in `SKILL.md`
  - style rules in `style-pack.md`
  - evidence trail in `reference-index.md`
  - deep grammar notes in `line-plane-intent-analysis.md`
  - anti-generic constraints inside the style documents, not the base skill

## Reference-derived style context

The reference set repeatedly points toward:

- elegant but dangerous character presence
- dark or high-contrast staging with selective luminous accents
- a collision of gothic, cyber, crystalline, floral, and architectural motifs
- restrained facial performance with highly controlled eye emphasis
- delicate skin rendering against harsher structure, costume, shard, or environment treatment

## Boundary rules

- Do not claim literal author training or model fine-tuning.
- Do not treat the folder as permission for direct living-artist imitation; use it as a local reference-trait pack.
- Do not overwrite the base skill.
- Keep derivative updates local to this folder unless the user explicitly asks to evolve the base methodology itself.
